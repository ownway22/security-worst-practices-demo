name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '29 21 * * 5'

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 60

    strategy:
      fail-fast: false
      matrix:
        language: [ 'python', 'javascript' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        # queries: security-extended,security-and-quality

    - name: Set up Python
      if: matrix.language == 'python'
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      if: matrix.language == 'python'
      run: |
        if [ -f requirements.txt ]; then pip install -r requirements.txt || true; fi
        if [ -f pyproject.toml ]; then pip install . || true; fi

    - name: Install Node dependencies
      if: matrix.language == 'javascript'
      run: |
        if [ -f package-lock.json ]; then npm ci || true; elif [ -f package.json ]; then npm install || true; fi

    - name: Autobuild (JavaScript)
      if: matrix.language == 'javascript'
      uses: github/codeql-action/autobuild@v3

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: '/language:${{matrix.language}}'
        # 將 SARIF 輸出到每個語言獨立資料夾，便於後續上傳 Artifact
        output: sarif-${{ matrix.language }}

    - name: Upload SARIF artifact
      uses: actions/upload-artifact@v4
      with:
        name: codeql-sarif-${{ matrix.language }}
        path: sarif-${{ matrix.language }}/*.sarif
        if-no-files-found: error
        retention-days: 7
